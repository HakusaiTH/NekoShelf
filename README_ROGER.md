# 📕 Work Guide for Roger (Member 5)

**Assigned Task:** Develop Design System & Theme Styling (`public/css/style.css`)  
**Your Git Branch:** `feature/theme-styles`

---

## 🤖 Copy & Paste the Prompt Below to your AI Assistant (Antigravity / ChatGPT / Claude):

```text
You are a coding assistant helping with the MVC Book Library project.

Your tasks:
1. Enhance and develop `public/css/style.css` into a Modern Design System:
   - Define CSS Variables for theme colors (Primary: #4F46E5, Secondary: #06B6D4, Background: #F8FAFC, Text: #0F172A, Accent: #F59E0B).
   - Style core UI components: `.btn`, `.form-control`, `.card`, `.table`, `.badge`, `.modal`.
   - Add smooth micro-animations for hover states and transitions.
   - Ensure full responsive design across mobile, tablet, and desktop screens.

2. Once `public/css/style.css` is updated, execute the following Git commands to push your work to GitHub:
   - Create and switch to branch: `git checkout -b feature/theme-styles`
   - Check status: `git status`
   - Stage files: `git add public/css/style.css`
   - Commit changes: `git commit -m "feat: enhance theme design system and responsiveness"`
   - Push to GitHub: `git push origin feature/theme-styles`
```

---

## 🚀 Manual Step-by-Step Workflow (If AI doesn't run Git automatically)

Open Terminal in VS Code (`Ctrl + ~`) and run:

1. **Create new Branch:**
   ```bash
   git checkout -b feature/theme-styles
   ```
2. **Edit `public/css/style.css` (generated by AI)**
3. **Commit & Push to GitHub:**
   ```bash
   git add public/css/style.css
   git commit -m "feat: enhance theme design system and responsiveness"
   git push origin feature/theme-styles
   ```
4. Notify Leader (Member 1) to review and merge your Pull Request!
