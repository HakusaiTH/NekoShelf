# 📘 Work Guide for Namo (Member 2)

**Assigned Task:** Redesign the Add Book page (`views/add.ejs`)  
**Your Git Branch:** `feature/add-book`

---

## 🤖 Copy & Paste the Prompt Below to your AI Assistant (Antigravity / ChatGPT / Claude):

```text
You are a coding assistant helping with the MVC Book Library project.

Your tasks:
1. Redesign and style `views/add.ejs` with a Modern Clean UI:
   - Keep the original `<form action="/books" method="POST">` structure and name attributes (`title`, `author`, `genre`, `published_year`) intact. Do NOT change input name attributes.
   - Center the Form Card on screen with sleek shadows and padding.
   - Include SVG Vector Icons for each input field (e.g. book title, author, genre, published year).
   - Style the "Save Book" button with hover effects, and provide a "Cancel" button linking back to the homepage (`/`).
   - Use CSS classes compatible with `/css/style.css`.

2. Once `views/add.ejs` is updated, execute the following Git commands to push your work to GitHub:
   - Create and switch to branch: `git checkout -b feature/add-book`
   - Check status: `git status`
   - Stage files: `git add views/add.ejs`
   - Commit changes: `git commit -m "feat: redesign add book form page UI"`
   - Push to GitHub: `git push origin feature/add-book`
```

---

## 🚀 Manual Step-by-Step Workflow (If AI doesn't run Git automatically)

Open Terminal in VS Code (`Ctrl + ~`) and run:

1. **Create new Branch:**
   ```bash
   git checkout -b feature/add-book
   ```
2. **Edit `views/add.ejs` (generated by AI)**
3. **Commit & Push to GitHub:**
   ```bash
   git add views/add.ejs
   git commit -m "feat: redesign add book form page UI"
   git push origin feature/add-book
   ```
4. Notify Leader (Member 1) to review and merge your Pull Request!
