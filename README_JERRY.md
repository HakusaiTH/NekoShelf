# 📗 Work Guide for Jerry (Member 4)

**Assigned Task:** Build Header and Footer Partials Components (`views/partials/`)  
**Your Git Branch:** `feature/layout-partials`

---

## 🤖 Copy & Paste the Prompt Below to your AI Assistant (Antigravity / ChatGPT / Claude):

```text
You are a coding assistant helping with the MVC Book Library project.

Your tasks:
1. Create Partials Components in `views/partials/`:
   - Create `views/partials/header.ejs`: Navigation Bar with 'BookHub' logo, search input UI, and '+ Add New Book' action button.
   - Create `views/partials/footer.ejs`: Footer with copyright information, social links, and team credits.
   - Use SVG Vector Icons instead of emojis.
   - Ensure responsive layout for both mobile and desktop screens.

2. Once files in `views/partials/` are created, execute the following Git commands to push your work to GitHub:
   - Create and switch to branch: `git checkout -b feature/layout-partials`
   - Check status: `git status`
   - Stage files: `git add views/partials/`
   - Commit changes: `git commit -m "feat: create navbar header and footer layout partials"`
   - Push to GitHub: `git push origin feature/layout-partials`
```

---

## 🚀 Manual Step-by-Step Workflow (If AI doesn't run Git automatically)

Open Terminal in VS Code (`Ctrl + ~`) and run:

1. **Create new Branch:**
   ```bash
   git checkout -b feature/layout-partials
   ```
2. **Create files in `views/partials/` (generated by AI)**
3. **Commit & Push to GitHub:**
   ```bash
   git add views/partials/
   git commit -m "feat: create navbar header and footer layout partials"
   git push origin feature/layout-partials
   ```
4. Notify Leader (Member 1) to review and merge your Pull Request!
