# 📙 Work Guide for Ammy (Member 3)

**Assigned Task:** Redesign the Edit Book page (`views/edit.ejs`)  
**Your Git Branch:** `feature/edit-book`

---

## 🤖 Copy & Paste the Prompt Below to your AI Assistant (Antigravity / ChatGPT / Claude):

```text
You are a coding assistant helping with the MVC Book Library project.

Your tasks:
1. Redesign and style `views/edit.ejs` with a Professional Modern UI:
   - Keep the original `<form action="/books/<%= book.id %>?_method=PUT" method="POST">` and pre-filled EJS values (`<%= book.title %>`, `<%= book.author %>`, `<%= book.genre %>`, `<%= book.published_year %>`) intact. Do NOT break form submission.
   - Layout the Form inside a centered Card container with clean labels and SVG input icons.
   - Style the "Update Book" button with hover effects and a "Cancel" button linking back to `/`.
   - Design a dedicated "Danger Zone" for the Delete Button with a confirmation modal/dialog before deleting.
   - Use CSS classes compatible with `/css/style.css`.

2. Once `views/edit.ejs` is updated, execute the following Git commands to push your work to GitHub:
   - Create and switch to branch: `git checkout -b feature/edit-book`
   - Check status: `git status`
   - Stage files: `git add views/edit.ejs`
   - Commit changes: `git commit -m "feat: redesign edit book form and delete section UI"`
   - Push to GitHub: `git push origin feature/edit-book`
```

---

## 🚀 Manual Step-by-Step Workflow (If AI doesn't run Git automatically)

Open Terminal in VS Code (`Ctrl + ~`) and run:

1. **Create new Branch:**
   ```bash
   git checkout -b feature/edit-book
   ```
2. **Edit `views/edit.ejs` (generated by AI)**
3. **Commit & Push to GitHub:**
   ```bash
   git add views/edit.ejs
   git commit -m "feat: redesign edit book form and delete section UI"
   git push origin feature/edit-book
   ```
4. Notify Leader (Member 1) to review and merge your Pull Request!
